#--------------------------------------------------------------
# Provider
#--------------------------------------------------------------

provider "aws" {
  region              = "${var.global_region}"
}

#--------------------------------------------------------------
# Pin terraform version
#--------------------------------------------------------------

terraform {
  required_version = "= 1.2.9"
}

